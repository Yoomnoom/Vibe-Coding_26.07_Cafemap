# CLAUDE.md

## 프로젝트 개요
엑셀로 정리한 카페 목록을 카카오맵에 마커로 표시하고, 로그인한
사용자의 방문 소감을 Supabase Database에 저장하는 지도 서비스.
(PRD.md 참고)

## 반드시 지킬 규칙
- 카카오맵 키는 `.env`의 `VITE_KAKAO_MAP_KEY`로 관리한다.
- 카카오맵 SDK는 `libraries=services`를 포함해서 불러온다
  (주소→좌표 변환에 필요).
- Supabase URL과 anon key는 `.env`의 `VITE_SUPABASE_URL`,
  `VITE_SUPABASE_ANON_KEY`로 관리한다.
- 소감 테이블은 RLS를 켜고 "본인 데이터만" 정책을 적용한다.
- 방문 소감 화면 데이터는 로그인한 사용자 단위로 완전히 분리한다. 다른 계정으로
  전환(로그아웃 후 재로그인 포함)하면 화면에서 이전 사용자의 방문 체크/소감이
  단 하나도 보여선 안 된다. 인증 상태가 바뀌면 방문 데이터를 즉시 초기화한 뒤
  새 사용자 것만 다시 불러온다(RLS는 DB 레벨 방어일 뿐, 클라이언트 state도
  같은 원칙으로 맞춰야 한다).
- `.env`는 `.gitignore`에 포함한다.
- 화면은 가짜 데이터로 먼저 만들고, 마지막에 실제 연동으로 교체한다.
- 마커를 다시 그릴 때는 이전 마커를 모두 지운 뒤 새로 그린다.
- 주소→좌표 변환은 하나씩 순서대로, 실패는 화면에 안내한다.
- 소감 저장은 upsert로, 같은 장소(이름+주소) 기록은 1개만 유지한다.
- Supabase 관련 작업(테이블, RLS)은 Supabase MCP 도구로 수행한다.

## 기술 스택
React + Vite, Tailwind + shadcn/ui, xlsx, 카카오맵 JS SDK,
Supabase(Auth/Database, MCP로 관리)

## 이 구조에서 흔한 실수 예방 (추가 권장 규칙)
- 카카오맵 SDK `<script>` 태그는 한 번만 로드한다. React 재렌더링/StrictMode의
  이중 실행 등으로 스크립트를 중복 삽입하면 `kakao.maps`가 이상 동작하거나
  지도가 여러 번 초기화될 수 있다. 로더 함수에 "이미 로드됐으면 재사용" 가드를 둔다.
- 지오코딩/마커 그리기 로직을 `useEffect`에 넣을 때 의존성 배열을 신중히 관리한다.
  카페 목록 상태가 바뀔 때마다 전체 재지오코딩이 도는 무한 루프/과다 API 호출을
  만들지 않도록, 좌표가 없는 카페에 대해서만 지오코딩을 실행한다.
- 좌표(`lat`/`lng`)는 DB에 숫자 타입(`numeric`/`double precision`)으로 저장한다.
  문자열로 저장하면 지도 라이브러리에 넘길 때 타입 오류나 위치 오차가 생길 수 있다.
- 카카오 지오코딩 결과는 `x = 경도(lng)`, `y = 위도(lat)`로 반환된다. 위/경도를
  서로 바꿔 저장하지 않도록 변환 지점에서 `lat`/`lng` 이름을 명확히 쓴다.
- 엑셀 파싱 시 셀 값의 앞뒤 공백 차이로 같은 카페가 다른 레코드로 upsert되는
  경우가 있다. `name`/`address`는 저장 전에 `trim()`해서 정규화한다.
- 수동 좌표 입력 UI에서는 값 검증(위도 -90~90, 경도 -180~180, 숫자 여부)을 거친다.
- 카카오맵 JS 키는 카카오 개발자 콘솔에서 Web 플랫폼 도메인 등록으로 제한한다.
  배포 도메인뿐 아니라 로컬 개발 도메인(`http://localhost:포트`)도 등록해야
  로컬 개발 중 지도가 정상 동작한다.
- 소감 테이블뿐 아니라 카페 목록 테이블 등 새로 만드는 모든 테이블에 기본으로
  RLS를 켠다(공개 조회용 테이블은 RLS를 켠 뒤 "누구나 SELECT" 정책을 명시 추가).
- DB에 `UNIQUE(user_id, cafe_id)` 제약을 걸어 upsert 중복 방지를 앱 로직뿐
  아니라 DB 레벨에서도 강제한다(동시 요청 등 레이스 컨디션 대비).
- Supabase 클라이언트 인스턴스는 앱 전체에서 하나만 생성해 재사용한다
  (파일마다 `createClient` 호출 반복 금지).
- 로그인 사용자(`user`)가 바뀔 때마다 다시 불러오는 `useEffect`는 반드시
  "즉시 초기화 + cancelled 가드" 패턴을 쓴다. 그렇지 않으면 이전 사용자의
  느린 응답이 나중에 도착해 새 사용자 state를 덮어쓰는 레이스 컨디션이
  생겨, 계정을 바꿔도 이전 사용자의 데이터가 화면에 남는 버그가 발생한다.
