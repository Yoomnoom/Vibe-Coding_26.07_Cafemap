# 우리 동네 카페 지도 서비스 — PRD (Product Requirements Document)

- 문서 버전: v1.2 (지역 정복 시스템 추가)
- 작성일: 2026-07-27 (최초), 2026-07-27 (v1.1), 2026-07-27 (v1.2)
- 작성자: (자동 생성 초안, 검토 후 확정 필요)

---

## 1. 개요 (Overview)

엑셀로 정리한 카페 목록(이름, 주소, 카테고리)을 업로드하면, 각 카페의 주소를 지도 좌표로 변환하여 카카오맵 위에 마커로 표시하는 웹 서비스. 사용자는 지도 위 마커를 눌러 방문 여부를 체크하고 한줄 소감을 남길 수 있으며, 로그인 시 본인의 기록이 저장되어 다음에 같은 카페를 눌러도 예전 소감이 그대로 보인다. 방문 체크한 카페만 모아보는 개인 목록 화면도 제공한다.

### 1.1 목표 (Goals)

- 엑셀 목록 → 지도 시각화까지의 과정을 최소 클릭으로 처리
- 지오코딩 실패 주소를 사용자가 인지하고 스스로 보정(수동 좌표 입력)할 수 있게 함
- 로그인한 사용자별로 "방문 여부 + 한줄 소감"을 안전하게(RLS) 저장/복원
- 동일 카페(이름+주소 기준)에 대한 개인 기록은 항상 1건만 유지(중복 방지)

### 1.2 비목표 (Non-Goals, 이번 범위에서 제외)

- 카페 평점/별점, 사진 업로드, 댓글/공유 기능
- 여러 명이 같은 카페에 남긴 소감을 서로 볼 수 있는 소셜 피드 기능(본인 소감만 조회)
- 엑셀 외 포맷(CSV, 구글시트 연동 등) 지원
- 관리자 페이지(엑셀 업로드 권한 제어 등) — 별도 논의 필요 (10장 "범위 밖" 참고)

---

## 2. 사용자 및 시나리오

### 2.1 대상 사용자

- 동네 카페를 정리해 둔 목록(엑셀)을 가진 개인/소모임 운영자
- 지도에서 카페 위치를 확인하고, 방문 여부와 소감을 기록하고 싶은 일반 사용자

### 2.2 핵심 사용자 흐름 (User Flow)

1. 접속 → 지도 화면 진입 (로그인 여부와 무관하게 지도/마커는 조회 가능)
2. 엑셀 업로드 → 카페 목록 파싱 → 지오코딩 → 지도에 마커 표시, 실패 주소는 안내 목록으로 별도 노출
3. (실패 주소가 있다면) 사용자가 위도/경도를 수동 입력해 마커 보정
4. 마커 클릭 → 상세 패널에서 방문 여부 체크 + 한줄 소감 입력
   - 비로그인 상태: 체크/소감 입력 시 로그인 유도
   - 로그인 상태: 즉시 저장, 같은 카페를 다시 클릭하면 저장된 내용이 자동으로 채워짐
5. "내가 방문한 카페" 목록 화면 → 로그인한 본인이 방문 체크한 카페만 리스트로 확인 (엑셀 재업로드 불필요)

---

## 3. 핵심 기능 요구사항

### 3.1 엑셀 업로드 및 카페 목록 파싱

- 지원 포맷: `.xlsx`
- 필수 컬럼: `이름`, `주소`, `카테고리` (헤더명 고정, 순서는 무관)
- 업로드된 카페 목록은 **DB에 영구 저장되는 공용 데이터**로 취급한다.
  - 모든 사용자(비로그인 포함)가 동일한 지도/마커를 조회한다.
  - 동일 카페(이름+주소 동일)가 재업로드되면 **upsert**(있으면 갱신, 없으면 추가) 처리한다.
  - 카페 데이터 자체(이름/주소/카테고리)는 RLS로 사용자별 분리하지 않는다. 분리 대상은 "방문 여부/소감"뿐이다.
- 업로드 가능 권한: **미정 — 5장 "확인 필요 사항" 참고**. 초기 버전은 로그인 사용자 누구나 업로드 가능한 것으로 가정.
- 파싱 실패(컬럼 누락, 빈 파일 등) 시 사용자에게 구체적인 에러 메시지 표시(몇 행에 어떤 문제인지)

### 3.2 지오코딩 및 마커 표시

- 주소 → 좌표 변환은 **프론트엔드에서 카카오맵 JS SDK의 `kakao.maps.services.Geocoder`를 직접 호출**한다.
  - 별도의 서버리스 지오코딩 함수는 두지 않는다(요청에 따른 결정).
  - 카카오맵 JS 키는 도메인 제한(카카오 개발자 콘솔의 "Web 플랫폼 등록")으로 보호하고, `.env` 및 Vercel 환경변수로 관리한다.
- 지오코딩 성공 시: 좌표를 카페 레코드에 저장(재조회 시 재지오코딩 불필요), 지도에 마커 표시
- 지오코딩 실패 시:
  - 지도가 아닌 **별도 "주소를 찾지 못한 카페" 안내 목록**에 표시(카페명 + 원본 주소 + 실패 사유)
  - 사용자가 해당 카페에 대해 **위도/경도를 직접 입력**하면 좌표가 저장되고 지도에 마커로 추가된다.
  - 수동 입력값의 형식 검증(위도 -90~90, 경도 -180~180)만 수행, 별도 검증 API는 두지 않는다.

### 3.3 마커 클릭 → 방문 체크 + 한줄 소감

- 마커 클릭 시 상세 패널(또는 인포윈도우)에 표시:
  - 카페 이름 / 주소 / 카테고리
  - 방문 여부 체크박스(또는 토글)
  - 한줄 소감 입력창(텍스트, 길이 제한 예: 200자)
  - 저장 버튼(또는 blur/체크 시 자동 저장)
- 비로그인 사용자가 체크/입력을 시도하면 로그인 화면으로 유도하고, 로그인 후 원래 카페로 돌아와 이어서 입력 가능해야 한다.

### 3.4 회원가입/로그인 및 개인 기록 저장

- 인증: **Supabase Auth**
  - 이메일 + 비밀번호 가입/로그인
  - 소셜 로그인(구글 등) 지원
- 로그인한 사용자가 특정 카페에 남긴 "방문 여부 + 소감"은 Supabase DB에 저장한다.
- 같은 카페를 다시 클릭하면, 그 사용자가 이전에 남긴 방문 여부/소감이 그대로 불러와져 표시된다.
- **동일 인물 + 동일 장소(이름+주소) 조합에 대한 기록은 항상 1건만 유지**한다.
  - DB 유니크 제약(예: `UNIQUE(user_id, cafe_id)`, 여기서 `cafe_id`는 이름+주소로 식별되는 카페 레코드)으로 강제한다.
  - 저장 로직은 upsert(있으면 UPDATE, 없으면 INSERT) 방식으로 동작한다.
- **방문 기록은 로그인한 사용자별로 화면에서도 완전히 분리되어야 한다.** 이 지도는 "로그인한 사용자 개인의 지도"이며, 로그인하는 이유 자체가 본인 기록을 관리하기 위함이다.
  - 다른 계정으로 전환(로그아웃 후 재로그인 포함)하면, 이전 계정이 체크/작성한 방문 여부·소감이 화면(지도 마커 색, 목록, 팝업)에 **한순간도** 남아 있으면 안 된다.
  - RLS는 DB 조회 자체를 막아주지만, 클라이언트 state도 사용자 전환 시 방문 데이터를 즉시 초기화하고 이전 사용자의 지연 응답이 새 사용자 상태를 덮어쓰지 않도록 구현해야 한다(실제로 이 부분에서 레이스 컨디션 버그가 있었음 — 이전 사용자 응답이 늦게 도착해 새 사용자 화면에 섞여 보임).

### 3.5 "방문한 카페" 목록 화면

- 로그인한 사용자가 방문 체크한 카페만 리스트 형태로 모아보는 화면
- 엑셀 재업로드 없이, DB에 저장된 공용 카페 목록 + 본인의 방문 기록을 조인하여 조회
- RLS에 의해 본인이 체크한 것만 조회되며, 다른 사용자의 방문 기록은 보이지 않는다.
- 목록 항목 클릭 시 지도 화면의 해당 마커 위치로 이동(선택 기능, 우선순위는 낮음)
- 구현 방식: 별도 화면(라우트)이 아니라, 메인 화면 카페 목록에 **"방문 여부 필터"(전체/방문함/안 가본 곳)**를 두어 같은 목적을 달성한다.

### 3.6 방문 마커 색 구분

- 방문 체크한 카페의 마커는 카테고리와 무관하게 고정된 색(진한 남색) + 체크 표시로 지도에서 바로 구분된다.
- 체크 해제 시 원래(카테고리) 색으로 즉시 복원된다.
- 색상만 바뀌며 좌표(lat/lng)는 변경되지 않는다.
- 목록의 방문 여부와 지도 마커 색은 항상 동일한 상태(`visit_notes` 기준)를 따른다.

### 3.7 카테고리별 마커 색 및 필터

- 카페 1건은 카테고리 1개만 가진다. 카테고리가 없으면 기본 회색을 사용한다.
- 카테고리 값은 **자유 텍스트**로 취급하며, 값(문자열)을 해시하여 고정 팔레트에서 색을 고른다(사전 정의된 카테고리 목록을 강제하지 않음 — 5장 open question #3 결론).
- 카테고리 필터(칩 버튼)로 지도와 목록을 동시에 특정 카테고리만 보이도록 좁힐 수 있다. "전체" 선택 시 원상 복귀.

### 3.8 방문 현황 요약

- 지도 상단에 "총 ○곳 중 ○곳 방문 완료" 형태로 표시.
- 총 개수/방문 개수는 카테고리 필터와 무관하게 **전체 카페 기준**으로 실시간 계산된다.
- 카페 추가(엑셀 업로드) 또는 방문 체크 변경 시 즉시 갱신된다.

### 3.9 목록 정렬

- 카페 목록에서 **최신순**(등록일 내림차순) / **이름순**(가나다) 정렬을 토글 버튼으로 전환한다.
- 정렬은 목록에만 적용되고 지도 마커 순서에는 영향을 주지 않는다.
- 선택한 정렬 방식은 브라우저 `localStorage`에 저장되어 새로고침 후에도 유지된다(로그인 여부와 무관, 기기/브라우저 단위로 유지).

### 3.10 지역 정복 시스템 (구 아이디어명: "나만의 카페 도장지도")

> 두 초안("나만의 카페 도장지도", "지역 정복 시스템(Area Conquest System)")이 같은 메커니즘(지역별 방문 진행률 + 100% 달성 시 정복 완료 표시 + 지역 필터)을 설명하고 있어 하나의 기능으로 통합한다. "도장"은 별도 데이터가 아니라 "방문 완료 카페"를 부르는 표현으로 취급한다.

**목표(Goal)**: 단순히 카페를 저장하는 것을 넘어, 지역별 방문 현황을 시각적으로 확인하며 카페를 "수집"하는 재미와 성취감을 제공한다.

**지역 판정 방식(기술 결정)**
- 지역 구분은 **카카오맵 좌표→행정구역 역지오코딩**(`kakao.maps.services.Geocoder.coord2RegionCode`)의 `region_3depth_name`(법정동/행정동, 예: "성수동1가")을 기준으로 한다.
- 엑셀 업로드 시 주소→좌표 지오코딩(3.2)이 끝난 직후, 같은 좌표로 좌표→행정구역 역지오코딩을 한 번 더 호출해 `region`을 함께 저장한다. 즉 카페 1건당 카카오 API를 2회 호출한다(순방향 지오코딩 + 역지오코딩), 3.2와 마찬가지로 하나씩 순차 처리한다.
- 역지오코딩 결과가 없거나 실패하면 해당 카페는 **"지역 미분류"** 그룹으로 묶는다(전체 카페/방문 집계에는 포함되지만 지역별 카드에는 별도 그룹으로 표시).
- **주의**: 사용자가 흔히 부르는 상권명("홍대", "연남동")과 카카오가 반환하는 공식 법정동명(예: "서교동")이 다를 수 있다. 이번 범위에서는 카카오가 반환하는 이름을 그대로 표시한다(사용자 지정 지역명 매핑은 11장 향후 확장 참고).
- 사용자가 지역 단위를 직접 지정하는 기능은 이번 범위에서 지원하지 않는다(카카오 자동 판정만 지원).

**핵심 요구사항(Requirements)**
- 지역별로 "저장된 카페 개수"와 "방문한 카페 개수(로그인한 본인 기준)"를 계산한다.
- 각 지역의 방문 진행률(%) = 방문 개수 / 전체 개수 × 100, 실시간으로 계산(별도 저장 없이 매번 파생 계산 — 카페 추가/삭제, 방문 체크 변경 시 항상 최신값).
- 지역별 진행률을 카드 형태로 제공한다(지역명, N/M 방문, 진행률 바, 남은 개수 또는 "정복 완료" 배지).
- 모든 카페를 방문한 지역(진행률 100%, 전체 1개 이상)은 **"정복 완료"** 상태로 표시하고 트로피/배지를 노출한다.
- 지역 카드를 선택하면 지도와 목록 모두 해당 지역 카페만 표시한다(기존 카테고리 필터·방문 여부 필터와 동시에 적용 가능해야 하며, AND 조건으로 결합된다).
- 동일 카페는 중복 집계하지 않는다(카페는 `cafes_260727`에 1건만 존재하므로 자연히 보장됨).
- 카페 추가(엑셀 업로드)·삭제 시 지역별 진행률이 자동으로 다시 계산된다. 단, **카페 삭제 기능 자체는 5장 open question #2("엑셀 재업로드 시 삭제 처리")가 아직 미해결**이라, 그 결정이 나야 "삭제 시 진행률 갱신"이 실제로 의미를 가진다.
- 기존 카페 저장(3.1)·방문 체크(3.3, 3.4) 기능과 충돌하지 않는다: 방문 여부는 여전히 `visit_notes_260727` 하나로만 관리하고, 지역 정복 시스템을 위한 별도의 방문 데이터를 새로 만들지 않는다(`region`은 `cafes_260727`에만 추가되는 파생 컬럼).

**검증(Acceptance Criteria)**
- [ ] 지역별 방문률(=진행률 %)이 정확하게 계산된다.
- [ ] 방문 체크/해제 시 해당 지역의 진행률이 즉시 갱신된다.
- [ ] 방문 완료(100%) 지역은 "정복 완료"(트로피/배지)로 표시된다.
- [ ] 새로고침 후에도 진행률·정복 상태가 유지된다(카페/방문 데이터가 DB에 있으므로 파생 계산만으로 자동 보장).
- [ ] 지도와 목록에 표시되는 지역별 진행률 정보가 서로 동일하다.
- [ ] 지역 카드를 선택하면 해당 지역 카페만 지도·목록에 표시되고, 카테고리/방문 여부 필터와 함께 적용해도 정상 동작한다.

**기대 효과(Expected Benefit)**
- 사용자가 지역별 방문 목표를 세우기 쉬워진다.
- 단순 기록 앱이 아닌 수집형 서비스 경험을 제공한다.
- 방문 현황을 직관적으로 확인할 수 있어 지속적인 사용을 유도한다.
- 특정 지역을 완성해 가는 성취감을 제공해 재방문을 유도한다.

---

## 4. 데이터 모델

> 이 Supabase 프로젝트는 다른 수업 실습들과 공유 중이라, 테이블명이 겹치지 않도록 뒤에 생성일(`260727`)을 붙였다. 실제 테이블명은 `cafes_260727`, `visit_notes_260727`.

### 4.1 `cafes_260727` (공용, 엑셀로 채워지는 카페 목록)

| 컬럼 | 설명 |
|---|---|
| id | PK |
| name | 카페 이름 |
| address | 주소(원본 텍스트) |
| category | 카테고리 |
| lat, lng | 지오코딩(또는 수동 입력)된 좌표. NULL 가능(=지오코딩 실패 상태) |
| geocode_status | `success` / `failed` / `manual` |
| region | 좌표→행정구역 역지오코딩 결과(`region_3depth_name`). NULL 가능(=지역 미분류, 3.10 참고) |
| created_at, updated_at | |

- 유니크 제약: `UNIQUE(name, address)` — 엑셀 재업로드 시 upsert 기준
- `region`은 사용자별로 다르지 않은 카페 고유 속성이므로 `visit_notes_260727`가 아니라 이 테이블에 저장한다(3.10 "기존 방문 기능과 별도 데이터를 만들지 않는다" 원칙).
- RLS: 조회(`SELECT`)는 전체 공개(비로그인 포함), 추가/수정(`INSERT`/`UPDATE`)은 로그인 사용자만 가능
- 참고: RLS 정책과 별개로, `anon`/`authenticated` 롤에 대한 테이블 GRANT(SELECT/INSERT/UPDATE)도 함께 부여해야 한다. SQL로 테이블을 직접 만들면 Supabase 대시보드가 자동으로 붙여주는 기본 GRANT가 빠지므로 마이그레이션에 명시적으로 포함해야 함(실제로 이 문제로 한 차례 "엑셀 업로드해도 반응 없음" 버그가 있었음).

### 4.2 `visit_notes_260727` (사용자별 방문 여부 + 소감)

| 컬럼 | 설명 |
|---|---|
| id | PK |
| user_id | FK → `auth.users.id` |
| cafe_id | FK → `cafes_260727.id` |
| visited | boolean |
| comment | 한줄 소감 (text) |
| created_at, updated_at | |

- 유니크 제약: `UNIQUE(user_id, cafe_id)` → "동일 인물 + 동일 장소(이름+주소)는 1건만" 요구사항을 DB 레벨에서 강제. (`cafe_id`가 `cafes_260727.(name, address)` 유니크 제약과 연결되어 있으므로, 결과적으로 "이름+주소" 기준 1인 1건이 보장됨)
- RLS 정책(요지):
  - `SELECT`: `auth.uid() = user_id` 인 행만 조회 가능(본인 기록만)
  - `INSERT` / `UPDATE`: `auth.uid() = user_id` 인 경우만 허용
  - `DELETE`: 필요 시 본인 기록만 허용(선택 기능)

---

## 5. 확인이 필요한 사항 (구현 착수 전 결정 필요)

아래 항목은 이번 답변으로 큰 방향은 정해졌으나, 세부 규칙은 구현 전에 확정이 필요합니다.

1. ~~**엑셀 업로드 권한**~~ — **결론: 로그인 사용자만 가능.** `cafes_260727`의 INSERT/UPDATE를 `authenticated` 롤에만 허용하는 RLS로 구현/적용 완료.
2. **엑셀 재업로드 시 삭제 처리**: 새 엑셀에 없는 기존 카페를 어떻게 할지 — 그대로 유지 / 비활성화 / 삭제? (기존 카페에 방문 기록이 걸려 있으면 삭제 시 문제 발생 가능) — **미해결.** 현재 구현은 재업로드된 카페를 기존 목록에 "추가/갱신"만 하고 삭제하지 않음(업로드 안 된 과거 카페도 계속 남음).
3. ~~**카테고리 값의 형태**~~ — **결론: 자유 텍스트.** 사전 정의 목록을 강제하지 않고, 문자열 해시로 마커/배지 색을 자동 배정.
4. **한줄 소감 글자 수 제한**: **결론: 200자**(구현 완료, `Textarea maxLength={200}`).
5. **비로그인 사용자의 지도 조회 범위**: 마커와 카페 정보(이름/주소/카테고리)까지는 공개, 그 외 방문/소감 통계(예: "N명이 다녀감")는 이번 범위에 포함하지 않는 것으로 가정 — 맞는지 확인 필요.
6. **카카오맵 JS 키 도메인 제한**: 배포 도메인(Vercel 프로덕션 + 프리뷰 URL 등)을 카카오 개발자 콘솔에 모두 등록해야 하는데, 프리뷰 URL처럼 매번 바뀌는 도메인은 어떻게 처리할지(예: 프리뷰 환경은 별도 카카오 앱 키 사용 등).
7. **소셜 로그인(구글)**: 3.4에 요구사항으로 명시돼 있으나 **아직 미구현**. Supabase 대시보드에서 Google OAuth Provider(클라이언트 ID/시크릿) 등록이 선행돼야 코드에서 버튼을 눌렀을 때 실제로 동작한다 — 8장 성공 기준에서도 제외된 상태.
8. **지역명 표기(3.10)**: 카카오 역지오코딩이 반환하는 공식 법정동명(예: "서교동")과 사용자가 흔히 쓰는 상권명("홍대")이 다른 경우가 많다. 이번 범위는 카카오 반환값 그대로 표기하는 것으로 결정했는데, 실제 사용자 테스트에서 혼란이 있으면 "상권명 매핑 테이블"을 추가로 만들지 결정 필요.
9. **지역 정복 시스템과 카페 삭제의 의존 관계(3.10)**: "카페 삭제 시 진행률 자동 갱신" 요구사항은 open question #2(엑셀 재업로드 시 삭제 처리)가 먼저 정해져야 완전히 검증 가능하다. #2가 "삭제 안 함"으로 결정되면 이 요구사항은 사실상 항상 참(카페가 안 지워지므로).

---

## 6. 기술 스택 및 아키텍처

- **프론트엔드**: 카카오맵 JS SDK를 이용해 지도 렌더링 + 지오코딩(`services.Geocoder`)까지 프론트에서 직접 처리
- **인증/DB**: Supabase (Auth + Postgres + RLS)
- **배포**: Vercel
- **환경변수 관리**:
  - 로컬 개발: `.env` (예: `NEXT_PUBLIC_KAKAO_MAP_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)
  - 배포: Vercel 프로젝트 환경변수에 동일 키 등록
  - 카카오맵 JS 키는 브라우저에 노출되는 것을 전제로 하며, 대신 카카오 개발자 콘솔의 "플랫폼(Web) 도메인 등록"으로 오남용을 제한한다.
  - Supabase는 `anon key` + RLS 정책으로 클라이언트 직접 접근을 안전하게 제한한다(서비스 롤 키는 클라이언트에 절대 노출하지 않음).

---

## 7. 화면 구성

1. **지도 화면(메인)**: 헤더(제목 + 엑셀 업로드 버튼 + 로그인/로그아웃) → 방문 현황 요약("총 ○곳 중 ○곳 방문 완료") → 카카오맵(카테고리/방문 색상 마커) → "주소를 찾지 못한 카페" 안내 패널 → 지역 정복 카드 목록(3.10) → 방문 여부 필터 / 카테고리 필터 칩 → 카페 목록(정렬 컨트롤 포함)
2. **카페 상세 팝업(Dialog)**: 마커 클릭 시 표시, 이름·주소 + 방문 체크 + 소감 입력 + 저장. 비로그인 시 로그인 안내로 대체.
3. **로그인/회원가입 팝업(Dialog)**: 이메일/비밀번호 탭 전환. 소셜 로그인 버튼은 5장 open question #7 참고(미구현).
4. **"방문한 카페만 보기"**: 별도 화면이 아니라 카페 목록의 "방문 여부 필터"로 구현(3.5 참고).
5. **지역 정복 카드**: 지역명, N/M 방문, 진행률 바, "정복까지 N곳 남음" 또는 "🏆 정복 완료" 배지. 카드 클릭 시 지도·목록이 해당 지역으로 필터링(3.10).

---

## 8. 성공 기준 (Acceptance Criteria 요약)

- 정상 형식 엑셀 업로드 시, 유효 주소를 가진 카페가 모두 지도에 마커로 표시된다.
- 지오코딩 실패 주소는 지도가 아닌 별도 안내 영역에 나열되고, 수동 좌표 입력 후 지도에 반영된다.
- 로그인 사용자가 특정 카페에 방문 체크 + 소감을 남기고 새로고침해도, 같은 카페를 다시 클릭하면 동일한 내용이 보인다.
- 동일 사용자가 동일 카페(이름+주소)에 대해 소감을 여러 번 저장해도 DB에는 항상 1건만 존재한다(수정으로 반영됨).
- 다른 사용자의 방문 여부/소감은 조회할 수 없다(RLS로 차단 확인).
- "방문한 카페" 목록 화면은 로그인한 본인이 체크한 카페만 보여준다.
- **계정 A로 방문 체크/소감을 남긴 뒤 로그아웃하고 계정 B로 로그인하면, 지도 마커 색·목록·팝업 어디에도 계정 A의 방문 여부/소감이 전혀 보이지 않는다.** (전환 즉시 초기화 확인)
- 방문 체크/해제 시 지도 마커 색이 즉시 바뀌고, 새로고침 후에도 유지된다.
- 카테고리 필터를 선택하면 지도와 목록에 해당 카테고리만 남고, 해제하면 전체로 복귀한다.
- 카페를 추가하거나 방문 체크를 바꾸면 "총 ○곳 중 ○곳 방문 완료" 숫자가 즉시 실제 데이터와 일치하게 갱신된다.
- 정렬 옵션(최신순/이름순)을 바꾸면 목록 순서가 즉시 바뀌고, 새로고침해도 선택한 정렬 방식이 유지된다.
- 지역별 방문률이 정확히 계산되고, 방문 체크 시 즉시 갱신되며, 100% 지역은 "정복 완료"로 표시된다.
- 새로고침 후에도 지역별 진행률/정복 상태가 유지되고, 지도·목록의 진행률 정보가 서로 일치한다.

---

## 9. 리스크 / 제약

- 카카오맵 지오코딩을 프론트에서 직접 호출하므로, 대량 엑셀(수백~수천 행) 업로드 시 클라이언트에서 순차/배치 호출에 따른 처리 시간이 길어질 수 있음 — 진행률 표시 UI 필요.
- 카카오 지오코딩 API의 요청 한도(쿼터)에 따라 대량 업로드 시 실패율이 늘어날 수 있음.
- 좌표가 없는 카페(geocoding 실패, 미보정)는 목록 화면에서 지도 이동 기능을 제공할 수 없음.
- 지역 정복 시스템(3.10)은 카페 1건당 역지오코딩 API를 추가로 1회 더 호출하므로, 대량 업로드 시 처리 시간과 카카오 API 쿼터 소모가 기존보다 늘어난다.
- 카카오 행정구역명과 사용자가 체감하는 동네 이름이 다를 수 있어("홍대" vs "서교동"), 지역 카드의 이름이 기대와 다르게 보일 수 있음(5장 open question #8).

---

## 10. 범위 밖 (추후 검토)

- 카페 이름/주소 텍스트 검색
- 지도 클러스터링(마커 수가 많아질 경우)
- 엑셀 업로드 이력 관리, 업로드 취소/롤백
- 관리자 전용 카페 데이터 관리 화면
- 소셜 로그인(구글) — 5장 open question #7 참고

---

## 11. 향후 확장 (Future) — 지역 정복 시스템 백로그

이번 범위(3.10)는 지역별 진행률 계산·정복 완료 표시·지역 필터까지만 구현한다. 아래는 이후 확장 후보로, 지금은 설계/구현하지 않고 아이디어만 남겨둔다.

- 🏆 지역별 배지 시스템 (예: "성수 마스터", "홍대 탐험가")
- ⭐ 정복 난이도 표시 (Easy / Medium / Hard)
- 📅 월간 정복 챌린지
- 🔥 정복률 랭킹 (내 기록)
- 🗺️ 서울 전체 정복률 (예: "서울 12% 완료")
- 🎁 정복 완료 애니메이션 및 축하 화면
- 🎯 "정복까지 N곳 남음" 추천 기능(3.10의 카드 표시와 별개로, 지도에서 가까운 미방문 카페를 추천)
- 📍 미방문 카페만 지도에 강조 표시
